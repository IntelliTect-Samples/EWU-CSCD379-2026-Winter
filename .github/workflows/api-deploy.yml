name: API CI/CD

on:
  push:
    branches:
      - kmilton1/Assignment4
  pull_request:
    branches:
      - kmilton1/Assignment4

jobs:
  build_test_deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "8.0.x"

      - name: Restore
        run: dotnet restore ./WorkFlow.Lite/src/api/WorkflowLite.api/WorkflowLite.Api.csproj

      - name: Test
        run: dotnet test ./WorkFlow.Lite/src/api/WorkflowLite.Tests/WorkflowLite.Tests.csproj -c Release

      - name: Publish
        run: dotnet publish ./WorkFlow.Lite/src/api/WorkflowLite.api/WorkflowLite.Api.csproj -c Release -o ./api_publish

      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v3
        with:
          app-name: ${{ secrets.AZURE_WEBAPP_NAME }}
          publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
          package: ./api_publish